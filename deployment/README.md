# Deployment examples

These files are inactive templates. Replace `CLIENT_DOMAIN`, `BACKEND_PORT`, and `PROJECT_PATH`; generate all credentials on the destination host. Keep each customer instance on its own database, media directory, service, and secret key.
